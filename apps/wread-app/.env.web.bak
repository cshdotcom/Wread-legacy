# WRead Environment Configuration
# For self-hosted deployment

NEXT_PUBLIC_APP_PLATFORM=web

# Server port (default 2586)
PORT=2586

# Local SQLite database path
WREAD_DB_PATH=./data/wread.db

# Local file storage path
WREAD_STORAGE_PATH=./data/storage

# JWT secret (change this in production!)
WREAD_JWT_SECRET=change-me-to-a-strong-random-secret-at-least-32-chars
WREAD_JWT_EXPIRY=7d

# Admin account (created on first startup)
WREAD_ADMIN_EMAIL=c135860@outlook.com
WREAD_ADMIN_PASSWORD=15068253855z

# Self-registration (default false, users added via DB only)
WREAD_ALLOW_SELF_REGISTER=false

# Storage quota (bytes, default 10GB; admin=unlimited when 0 in DB)
STORAGE_FIXED_QUOTA=10737418240

# Daily translation quota (characters, default 500k)
TRANSLATION_FIXED_QUOTA=500000

# LibreTranslate URL (self-hosted at ta.910500.xyz, displayed as 云翻译)
LIBRETRANSLATE_URL=https://ta.910500.xyz

# SMTP email config (for Send to WRead feature)
WREAD_SMTP_HOST=
WREAD_SMTP_PORT=587
WREAD_SMTP_USER=
WREAD_SMTP_PASS=
WREAD_SMTP_FROM=

# IMAP email config (for receiving Send to WRead emails)
WREAD_IMAP_HOST=
WREAD_IMAP_PORT=993
WREAD_IMAP_USER=
WREAD_IMAP_PASS=

# PDF.js paths (for development)
PDFJS_BUILD_PATH=../../packages/foliate-js/node_modules/pdfjs-dist/legacy/build
PDFJS_FONTS_PATH=../../packages/foliate-js/node_modules/pdfjs-dist
PDFJS_STYLE_PATH=../../packages/foliate-js/vendor/pdfjs
